pipeline {
  agent any
  stages {
    stage ('Build')
      steps {
        sh 'echo "Hello World #2"'
        sh '''
          echo "Mutiline shell steps works too"
          ls -lah
         '''
        }
       }
      }
     }
